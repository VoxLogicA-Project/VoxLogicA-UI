@tailwind base;
@tailwind components;
@tailwind utilities;
@tailwind variants;

@layer components {
	/* Fix for checkbox styling */
	[type='checkbox'] {
		@apply !accent-primary-500;
	}

	/* Make all text non-selectable by default */
	* {
		@apply select-none;
	}
}
